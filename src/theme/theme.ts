import { createTheme } from '@mantine/core'

export const erpTheme = createTheme({
  primaryColor: 'teal',
  defaultRadius: 'md',
  fontFamily: 'Public Sans, Segoe UI, sans-serif',
  headings: {
    fontFamily: 'Space Grotesk, Public Sans, Segoe UI, sans-serif',
  },
})
